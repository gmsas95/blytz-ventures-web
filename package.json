{
  "name": "blytz-ventures-web",
  "type": "module",
  "version": "2.0.0",
  "scripts": {
    "dev": "wrangler types && astro dev",
    "build": "wrangler types && astro build",
    "preview": "wrangler types && astro preview",
    "check": "wrangler types && astro check",
    "d1:migrate:local": "wrangler d1 migrations apply blytz-ventures-db --local",
    "d1:migrate:remote": "wrangler d1 migrations apply blytz-ventures-db --remote"
  },
  "dependencies": {
    "@astrojs/cloudflare": "^10.4.2",
    "@astrojs/tailwind": "^5.1.0",
    "astro": "^4.16.0",
    "tailwindcss": "^3.4.0",
    "wrangler": "^4.81.0"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.8",
    "@tailwindcss/typography": "^0.5.10",
    "typescript": "^5.3.0"
  }
}
